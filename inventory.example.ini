# Copy to inventory.ini.
[radio_host]
radio-host.example.internal ansible_user=deploy

# The NFS server holding the music library. Playlist generation and the
# metadata scan run here (delegate_to), next to the files.
[nas]
nas ansible_host=nas.example.internal ansible_user=deploy
